export interface ILoginResponse {
  accessToken: string;
  refreshToken: string;
}

export interface IOption {
  value: string;
  label: string;
}

export interface ILatLng {
  lat: number;
  lng: number;
}
